# ╔══════════════════════════════════════════════════════════════════════════╗
# ║  OpenClaw Companion — Environment Configuration                        ║
# ║  Copy to .env and edit with your values: cp .env.example .env          ║
# ╚══════════════════════════════════════════════════════════════════════════╝

# ─── Required ─────────────────────────────────────────────────────────────

# OpenClaw Gateway WebSocket URL
GATEWAY_WS_URL=ws://127.0.0.1:18789

# OpenClaw Gateway auth token (from openclaw.json → gateway.auth.token)
GATEWAY_TOKEN=your-gateway-token-here

# Shared secret between app and voice server
AUTH_TOKEN=change-me-to-something-secret

# ─── Speech-to-Text (whisper-fast) ───────────────────────────────────────

# Whisper model
WHISPER_MODEL=Systran/faster-whisper-large-v3-turbo

# STT device: cuda (GPU, ~239ms) or cpu (~2-3s)
STT_DEVICE=cuda

# Compute type: int8 (fastest), float16, int8_float16
STT_COMPUTE_TYPE=int8

# Docker image tag: latest-cuda (GPU) or latest (CPU)
STT_TAG=latest-cuda

# HuggingFace model cache path (host path for volume mount)
# First run: model downloads here (~1.6GB). Subsequent runs use cache.
HF_CACHE_PATH=/home/ubuntu/.cache/huggingface

# STT port
STT_PORT=9000

# ─── Text-to-Speech ──────────────────────────────────────────────────────

# TTS engine: "edge" (cloud, free) or "kokoro" (local GPU, faster)
TTS_ENGINE=kokoro

# Edge TTS voice (fallback)
TTS_VOICE=es-AR-TomasNeural

# Kokoro settings
KOKORO_VOICE=em_alex
KOKORO_VOICE_EN=bm_george
KOKORO_PORT=5004
KOKORO_TAG=latest-gpu

# ─── Voice Server ────────────────────────────────────────────────────────

BOT_NAME=Jarvis
OWNER_NAME=User
VOICE_SESSION_KEY=voice
VOICE_PORT=3200
USE_GATEWAY_WS=true

# WSS (optional, requires TLS certs)
# WSS_PORT=3443
# TLS_CERT=/certs/cert.pem
# TLS_KEY=/certs/key.pem

# ─── Google Meet Bot (optional: docker compose --profile meet up -d) ─────

MEET_SESSION_KEY=meet
MEET_BOT_IMAGE=meet-bot:v6
LIVE2D_MODEL=wanko
LIVE2D_ENABLED=true
DEFAULT_LANG=es
MEET_PORT=3300
SPEAKER_ID=true
VAD_CHUNK_MS=1500

# ─── Meet Orchestrator (multi-meeting support) ──────────────────────────

# Max concurrent meetings
MAX_MEETINGS=5

# Docker socket path (for spawning meet-worker containers)
# DOCKER_SOCKET=/var/run/docker.sock

# Google Calendar ICS URL (optional, enables auto-join)
# GOOGLE_CALENDAR_ICS=https://calendar.google.com/calendar/ical/your-calendar/basic.ics
