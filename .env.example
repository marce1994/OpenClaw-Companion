# ╔══════════════════════════════════════════════════════════════════════════╗
# ║  OpenClaw Companion — Environment Configuration                        ║
# ║  Copy to .env and edit with your values: cp .env.example .env          ║
# ╚══════════════════════════════════════════════════════════════════════════╝

# ─── Required ─────────────────────────────────────────────────────────────

# OpenClaw Gateway WebSocket URL (for native WS integration)
GATEWAY_WS_URL=ws://127.0.0.1:18789

# OpenClaw Gateway auth token (find in your openclaw.json → gateway.auth.token)
GATEWAY_TOKEN=your-gateway-token-here

# Shared secret between app and voice server (set anything unique)
AUTH_TOKEN=change-me-to-something-secret

# ─── Speech-to-Text (Speaches / faster-whisper) ──────────────────────────

# Whisper model (default is best speed/quality balance)
WHISPER_MODEL=Systran/faster-whisper-large-v3-turbo

# HuggingFace token — required for Systran gated model
# Get one at https://huggingface.co/settings/tokens (Read access)
# Without token, use: deepdml/faster-whisper-large-v3-turbo-ct2 (same weights, ungated)
HF_TOKEN=

# STT port (default 9000)
STT_PORT=9000

# ─── Text-to-Speech ──────────────────────────────────────────────────────

# TTS engine: "edge" (cloud, free) or "kokoro" (local, faster with GPU)
TTS_ENGINE=kokoro

# Edge TTS voice (fallback, see: edge-tts --list-voices)
TTS_VOICE=es-AR-TomasNeural

# Kokoro settings (when TTS_ENGINE=kokoro)
KOKORO_VOICE=em_alex
# KOKORO_VOICE_EN=bm_george    # English voice for Meet bot
KOKORO_PORT=5004

# ─── Voice Server ────────────────────────────────────────────────────────

# Bot name (used as wake word in Smart Listen)
BOT_NAME=assistant

# Your name (for speaker identification)
OWNER_NAME=User

# Voice session key (separate from Telegram main session)
VOICE_SESSION_KEY=voice

# WebSocket ports
VOICE_PORT=3200
# WSS_PORT=3443              # Uncomment for TLS (requires certs)
# TLS_CERT=/certs/cert.pem
# TLS_KEY=/certs/key.pem

# Gateway integration (recommended: true for persistent sessions)
USE_GATEWAY_WS=true

# ─── Google Meet Bot (optional, --profile meet) ──────────────────────────

# Meet session key (separate from voice session)
MEET_SESSION_KEY=meet

# Live2D avatar model: wanko (dog), Mao, Hiyori, Rice
LIVE2D_MODEL=wanko
LIVE2D_ENABLED=true

# Default language for Meet transcription
DEFAULT_LANG=es

# Meet bot HTTP API port
MEET_PORT=3300

# ─── Speaker Diarization (optional, --profile diarizer) ──────────────────

# Diarizer WebSocket port
DIARIZER_PORT=3202

# Latency vs accuracy tradeoff (seconds, lower = faster but less accurate)
DIARIZER_LATENCY=2.0
DIARIZER_STEP=0.5
