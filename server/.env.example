# ============================================================
# OpenClaw Companion — Voice Server Configuration
# ============================================================
# Copy to .env and edit: cp .env.example .env
# Or run ./setup.sh to generate it interactively.

# ===== REQUIRED: OpenClaw Gateway =====
GATEWAY_WS_URL=ws://localhost:18789
GATEWAY_TOKEN=your-gateway-token-here
USE_GATEWAY_WS=true

# ===== Voice Server Auth =====
# Clients must send this token to connect. Auto-generated if empty.
AUTH_TOKEN=

# ===== Speech-to-Text (Whisper) =====
# With docker-compose (host network): http://127.0.0.1:9000
WHISPER_URL=http://127.0.0.1:9000/asr?language=es&output=json

# ===== Text-to-Speech =====
# Engine: edge (cloud, no GPU) | kokoro (local, GPU recommended)
TTS_ENGINE=edge
# Edge voice — run `edge-tts --list-voices` for full list
TTS_VOICE=es-AR-TomasNeural
# Kokoro (only if TTS_ENGINE=kokoro)
KOKORO_URL=http://127.0.0.1:5004
KOKORO_VOICE=em_alex

# ===== Bot Identity =====
BOT_NAME=jarvis
OWNER_NAME=Pablo
GW_SESSION_KEY=voice

# ===== Whisper Model =====
# Options: tiny, base, small, medium, large-v3, large-v3-turbo
ASR_MODEL=large-v3-turbo

# ===== Docker Images (set by setup.sh) =====
WHISPER_IMAGE=onerahmet/openai-whisper-asr-webservice:latest
# GPU variant: onerahmet/openai-whisper-asr-webservice:latest-gpu
# KOKORO_IMAGE=ghcr.io/remsky/kokoro-fastapi-cpu:latest
# GPU variant: ghcr.io/remsky/kokoro-fastapi-gpu:latest

# ===== Optional: WSS (TLS) =====
# WSS_PORT=3443
# TLS_CERT=/certs/cert.pem
# TLS_KEY=/certs/key.pem

# ===== Ports =====
PORT=3200
WHISPER_PORT=9000
# KOKORO_PORT=5004
